meta {
  name: Forget password
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/api/forget-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{email}}"
  }
}

settings {
  encodeUrl: true
}
